import 'package:flutter/material.dart';

class Mistakes extends StatelessWidget {
  const Mistakes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Mistakes: 这个页面老子正在开发"));
  }
}
